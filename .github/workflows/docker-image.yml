name: Build and Publish Docker Image

permissions:
  contents: read
  packages: write

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - 'feature/*'

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 1. Приводим OWNER к нижнему регистру и сохраняем в $LOWER_OWNER
      - name: Set lower-case owner
        shell: bash
        run: |
          echo "LOWER_OWNER=$(echo \"$GITHUB_REPOSITORY_OWNER\" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Log in to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ env.LOWER_OWNER }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install pnpm & build
        run: |
          npm install -g pnpm
          pnpm install
          pnpm run build

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ env.LOWER_OWNER }}/skills-publish-packages:latest
            ghcr.io/${{ env.LOWER_OWNER }}/skills-publish-packages:sha-${{ github.sha }}
